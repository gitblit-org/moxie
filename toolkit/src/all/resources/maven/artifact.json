{
  "name": "${artifact.name}",
  "description": "${artifact.description}",
  "groupId": "${artifact.groupId}",
  "artifactId": "${artifact.artifactId}",
  "version": "${artifact.version}",
  "date": "${artifact.date}",
  "files": {
    "pom": { "path": "${artifact.pom}" },
    "package": { "path": "${artifact.package}", "size":"${artifact.packageSize}" },
    "sources": { "path": "${artifact.sources}", "size":"${artifact.sourcesSize}" },
    "javadoc": { "path": "${artifact.javadoc}", "size":"${artifact.javadocSize}" }
   }
}