name: ${groupId}:${artifactId}
description: project for ${groupId}:${artifactId}
groupId: ${groupId}
artifactId: ${artifactId}
version: ${version}
type: jar
apply: eclipse, cache
sourceFolders:
- compile src/main/java
- compile src/main/resources
- test src/test/java
- test src/test/resources
dependencies:
- test junit
- build cobertura